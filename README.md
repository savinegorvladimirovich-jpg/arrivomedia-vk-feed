# ArrivoMedia VK RSS for SMMplanner

Upload these files to the root of a GitHub repository and enable GitHub Pages.

Expected Pages URL:

`https://USERNAME.github.io/arrivomedia-vk-feed/feed.xml`

Before connecting the feed to SMMplanner, replace `USERNAME` in `feed.xml` with the real GitHub username.
